import './Todo.css'
function Todo({name,todoDelete,para}){

function todoCancel(){
  setModal(false)
        <h2>{ name}</h2>
        <p>{para}</p>
        <button onClick={todoDelete}>Delete</button>
        </div> 
        
)
}
export default Todo